# cms
## What This Project Does
This project appears to be a Content Management System (CMS) with distinct frontends for an administrative interface and a public-facing website. It likely allows for content creation, editing, and management, with separate views for administrators and regular users. The project structure suggests a separation of concerns between content management and public display.
## Tech Stack
*   TypeScript
*   Vite (likely for frontend bundling)
## Project Structure
The project is organized into two primary directories:
*   `mainSite/`: Contains the code for the public-facing website.
*   `adminSite/`: Contains the code for the administrative dashboard.
*   `.git`:  Standard Git repository directory.
*   `.gitignore`: Specifies intentionally untracked files that Git should ignore.
*   `README.md`: This file.
## Getting Started
Based on the existing README, this project likely uses Vite for building.  To get started, you would typically:
1.  Navigate to the project directory.
2.  Run `npm install` or `yarn install` to install dependencies.
3.  Run `npm run build` or `yarn build` to build the frontend.  (Specific commands may vary based on the project's `package.json`.)
## Status
Active
## Notes
This project is a Git repository hosted on GitHub at `https://github.com/danibarker/cms.git`. The existing README indicates the use of Vite and TypeScript, but further investigation of the `package.json` and project files would be needed to confirm build processes and dependencies. The project appears to be split into two distinct frontend applications.
